import React from "react";
import CustomerNavBar from '../CustomerNavBar';

export const Orders = () => {
    return (
        <div>
            <CustomerNavBar />
            {"This is order"}
        </div>

    );
};